%h1 Создать новый расклад

-if(@all_clients.length > 0)
  = render 'form'
-else
  %p
    Прежде чем добавлять расклады, вам необходимо
    =link_to new_client_path, 'добавить хотя бы одного клиента.'

= link_to 'К списку раскладов', spreads_path
